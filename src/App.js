import React from 'react';
import Stadium from './components/stadium/Stadium';

const App = () => (
    <main>
        <div>Stadium Seat Picker</div>
        <Stadium submitStand={() => {}} />
    </main>
);

export default App;
